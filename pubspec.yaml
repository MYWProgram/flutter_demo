name: flutter_demo
description: A new Flutter project including flutter demo by _3Y3.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # 屏幕适配
  flutter_screenutil: 5.0.0
  # 抖动动画组件
  shake_animation_widget: 2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
